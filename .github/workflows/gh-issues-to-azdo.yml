name: Issue Created Handler

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

jobs:
  handle-new-issue:
    if: github.event_name == 'issues'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Python Markdown
        run: pip install markdown

      - name: Determine Work Item Type
        id: determine-work-item-type
        uses: actions/github-script@v6
        with:
          script: |
            const labels = context.payload.issue.labels.map(label => label.name.toLowerCase());
            let workItemType = "";
            if (labels.includes("bug")) {
              workItemType = "Bug";
            } else if (labels.includes("enhancement")) {
              workItemType = "User Story";
            } else {
              workItemType = "Customer Escalation";
            }
            const encodedType = encodeURIComponent(workItemType);
            core.setOutput('display_type', workItemType);
            return encodedType;
          result-encoding: string

      - name: Create work item in Azure DevOps
        id: create-work-item
        env:
          AZDO_ORG: ${{ secrets.AZDO_ORG }}
          AZDO_PROJECT: ${{ secrets.AZDO_PROJECT }}
          AZURE_DEVOPS_TOKEN: ${{ secrets.AZURE_DEVOPS_TOKEN }}
          AZDO_AREA_PATH: ${{ secrets.AZDO_AREA_PATH }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          WORK_ITEM_TYPE: ${{ steps.determine-work-item-type.outputs.result }}
        run: |
          echo "Creating Azure DevOps work item for GitHub Issue: $ISSUE_TITLE"

          FORMATTED_ISSUE_BODY=$(python -c "
          import sys, markdown, re

          html = markdown.markdown(sys.stdin.read())

          def replace_img(match):
              src = re.search(r'src=\"([^\"]+)\"', match.group(0))
              alt = re.search(r'alt=\"([^\"]+)\"', match.group(0))
              src_val = src.group(1) if src else ''
              alt_val = alt.group(1) if alt else 'image'
              return f'__IMG_PLACEHOLDER__{src_val}__ALT__{alt_val}__END__'

          print(re.sub(r'<img[^>]*>', replace_img, html))
          " <<< "$ISSUE_BODY")

          DESCRIPTION="<div><p><strong>ISSUE DETAILS:</strong></p><p>Issue Number: $ISSUE_NUMBER</p><p><a href=\"$ISSUE_URL\">View Issue on GitHub</a></p><p>$FORMATTED_ISSUE_BODY</p><p style=\"color:red;\"><strong>NOTE:</strong> Please ensure to remove the \"CommentOnGitHub\" tag <u>if no action is required</u> for the work item.</p></div>"

          PATCH_DOC=$(jq -n \
            --arg title "GitHub Issue #$ISSUE_NUMBER: $ISSUE_TITLE" \
            --arg description "$DESCRIPTION" \
            --arg areaPath "$AZDO_AREA_PATH" \
            '[{"op": "add", "path": "/fields/System.Title", "value": $title},
              {"op": "add", "path": "/fields/System.Description", "value": $description},
              {"op": "add", "path": "/fields/System.AreaPath", "value": $areaPath},
              {"op": "add", "path": "/fields/System.Tags", "value": "CommentOnGitHub"}]')

          if [[ "$WORK_ITEM_TYPE" == "Customer%20Escalation" ]]; then
            PATCH_DOC=$(echo "$PATCH_DOC" | jq '. + [{"op": "add", "path": "/fields/Custom.EscalationPriority", "value": "Priority 3 - Medium"}, {"op": "add", "path": "/fields/Custom.Requestor", "value": "Semi BU"}, {"op": "add", "path": "/fields/Custom.BUAffinity", "value": "Semi/Elec"}]')
          fi

          if [[ "$WORK_ITEM_TYPE" == "Bug" ]]; then
            STS_VERSION=$(echo "$ISSUE_BODY" | sed -n '/System Setup/,$p' | grep -oP '(?<=STS Software Version )\d+\.\d+(\.\w+)?' | head -n 1 | tr -d '\r')
            if [[ -n "$STS_VERSION" ]]; then
              REPORTED_IN="STS Software Bundle $STS_VERSION"
            else
              REPORTED_IN="STS Software Bundle"
            fi
            echo "Extracted STS Software Version: $STS_VERSION"
            echo "Final ReportedIn value: $REPORTED_IN"
            PATCH_DOC=$(echo "$PATCH_DOC" | jq --arg reportedIn "$REPORTED_IN" '. + [{"op": "add", "path": "/fields/Custom.ReportedIn", "value": $reportedIn}]')
          fi

          echo "$PATCH_DOC" > patch.json

          WORK_ITEM_API_URL="https://dev.azure.com/$AZDO_ORG/$AZDO_PROJECT/_apis/wit/workitems/\$${WORK_ITEM_TYPE}?api-version=6.0"
          echo "Work Item API URL: $WORK_ITEM_API_URL"

          RESPONSE=$(curl -s -u :$AZURE_DEVOPS_TOKEN \
            -X POST \
            -H "Content-Type: application/json-patch+json" \
            --data @patch.json \
            "$WORK_ITEM_API_URL")

          WORK_ITEM_ID=$(echo "$RESPONSE" | jq -r '.id')
          if [ "$WORK_ITEM_ID" = "null" ]; then
            echo "Error: Work item creation failed."
            exit 1
          fi

          WORK_ITEM_URL="https://dev.azure.com/$AZDO_ORG/$AZDO_PROJECT/_workitems/edit/$WORK_ITEM_ID"
          echo "work_item_id=$WORK_ITEM_ID" >> $GITHUB_OUTPUT
          echo "work_item_url=$WORK_ITEM_URL" >> $GITHUB_OUTPUT
          echo "$FORMATTED_ISSUE_BODY" > formatted_body.txt

      - name: Upload Images and Update Work Item Description
        env:
          AZDO_ORG: ${{ secrets.AZDO_ORG }}
          AZDO_PROJECT: ${{ secrets.AZDO_PROJECT }}
          AZURE_DEVOPS_TOKEN: ${{ secrets.AZURE_DEVOPS_TOKEN }}
          WORK_ITEM_ID: ${{ steps.create-work-item.outputs.work_item_id }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
        run: |
          echo "Processing images for Work Item #$WORK_ITEM_ID"

          FORMATTED_BODY=$(cat formatted_body.txt)
          UPDATED_DESCRIPTION="$FORMATTED_BODY"
          IMG_INDEX=0

          # Extract all image placeholders and process them
          while IFS= read -r line; do
            SRC=$(echo "$line" | grep -oP '(?<=__IMG_PLACEHOLDER__)[^_]+(?=__ALT__)')
            ALT=$(echo "$line" | grep -oP '(?<=__ALT__)[^_]+(?=__END__)')

            if [[ -n "$SRC" ]]; then
              IMG_INDEX=$((IMG_INDEX + 1))
              IMG_FILE="image_$IMG_INDEX.png"

              echo "Downloading image $IMG_INDEX: $SRC"
              HTTP_STATUS=$(curl -sL -o "$IMG_FILE" -w "%{http_code}" "$SRC")

              if [[ "$HTTP_STATUS" == "200" ]] && [[ -s "$IMG_FILE" ]]; then
                echo "Uploading image $IMG_INDEX to AzDO"

                # Upload image to AzDO as attachment
                UPLOAD_RESPONSE=$(curl -s -u :$AZURE_DEVOPS_TOKEN \
                  -X POST \
                  -H "Content-Type: application/octet-stream" \
                  --data-binary @"$IMG_FILE" \
                  "https://dev.azure.com/$AZDO_ORG/$AZDO_PROJECT/_apis/wit/attachments?fileName=$IMG_FILE&api-version=6.0")

                echo "Upload response: $UPLOAD_RESPONSE"

                ATTACHMENT_URL=$(echo "$UPLOAD_RESPONSE" | jq -r '.url')

                if [[ "$ATTACHMENT_URL" != "null" && -n "$ATTACHMENT_URL" ]]; then
                  echo "Image $IMG_INDEX uploaded successfully: $ATTACHMENT_URL"

                  # Replace placeholder with actual inline AzDO image tag
                  AZDO_IMG_TAG="<img src=\"$ATTACHMENT_URL\" alt=\"$ALT\" style=\"max-width:100%;\" />"
                  PLACEHOLDER="__IMG_PLACEHOLDER__${SRC}__ALT__${ALT}__END__"
                  UPDATED_DESCRIPTION="${UPDATED_DESCRIPTION//$PLACEHOLDER/$AZDO_IMG_TAG}"
                else
                  echo "Failed to upload image $IMG_INDEX, using link as fallback"
                  FALLBACK="<a href=\"$SRC\">[Image: $ALT]</a>"
                  PLACEHOLDER="__IMG_PLACEHOLDER__${SRC}__ALT__${ALT}__END__"
                  UPDATED_DESCRIPTION="${UPDATED_DESCRIPTION//$PLACEHOLDER/$FALLBACK}"
                fi
              else
                echo "Failed to download image $IMG_INDEX (HTTP $HTTP_STATUS), using link as fallback"
                FALLBACK="<a href=\"$SRC\">[Image: $ALT]</a>"
                PLACEHOLDER="__IMG_PLACEHOLDER__${SRC}__ALT__${ALT}__END__"
                UPDATED_DESCRIPTION="${UPDATED_DESCRIPTION//$PLACEHOLDER/$FALLBACK}"
              fi
            fi
          done < <(echo "$FORMATTED_BODY" | grep -o '__IMG_PLACEHOLDER__[^_]*__ALT__[^_]*__END__')

          # Wrap final description
          FINAL_DESCRIPTION="<div><p><strong>ISSUE DETAILS:</strong></p><p>Issue Number: $ISSUE_NUMBER</p><p><a href=\"$ISSUE_URL\">View Issue on GitHub</a></p><p>$UPDATED_DESCRIPTION</p><p style=\"color:red;\"><strong>NOTE:</strong> Please ensure to remove the \"CommentOnGitHub\" tag <u>if no action is required</u> for the work item.</p></div>"

          # Update work item description with images
          UPDATE_PATCH=$(jq -n \
            --arg description "$FINAL_DESCRIPTION" \
            '[{"op": "replace", "path": "/fields/System.Description", "value": $description}]')

          echo "$UPDATE_PATCH" > patch-update.json

          UPDATE_RESPONSE=$(curl -s -u :$AZURE_DEVOPS_TOKEN \
            -X PATCH \
            -H "Content-Type: application/json-patch+json" \
            --data @patch-update.json \
            "https://dev.azure.com/$AZDO_ORG/$AZDO_PROJECT/_apis/wit/workitems/$WORK_ITEM_ID?api-version=6.0")

          if echo "$UPDATE_RESPONSE" | jq -e '.id' > /dev/null; then
            echo "Work item description updated with $IMG_INDEX image(s)"
          else
            echo "Failed to update work item description"
            echo "Response: $UPDATE_RESPONSE"
            exit 1
          fi

      - name: Link GitHub Issue to Azure DevOps Work Item
        env:
          AZDO_ORG: ${{ secrets.AZDO_ORG }}
          AZDO_PROJECT: ${{ secrets.AZDO_PROJECT }}
          AZURE_DEVOPS_TOKEN: ${{ secrets.AZURE_DEVOPS_TOKEN }}
          WORK_ITEM_ID: ${{ steps.create-work-item.outputs.work_item_id }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          GH_REPO_GUID: ${{ secrets.GH_REPO_GUID }}
        run: |
          echo "Linking GitHub Issue to the Azure DevOps Work Item"
          PATCH_RELATION=$(jq -n \
            --arg issueNumber "$ISSUE_NUMBER" \
            --arg repoGuid "$GH_REPO_GUID" \
            '[{"op": "add", "path": "/relations/-", "value": {"rel": "ArtifactLink", "url": "vstfs:///GitHub/Issue/\($repoGuid)%2F\($issueNumber)", "attributes": {"name": "GitHub Issue"}}}]')
          echo "$PATCH_RELATION" > patch-relation.json
          WORK_ITEM_API_URL="https://dev.azure.com/$AZDO_ORG/$AZDO_PROJECT/_apis/wit/workitems/$WORK_ITEM_ID?api-version=6.0"
          RESPONSE=$(curl -s -u :$AZURE_DEVOPS_TOKEN \
            -X PATCH \
            -H "Content-Type: application/json-patch+json" \
            --data @patch-relation.json \
            "$WORK_ITEM_API_URL")

      - name: Update GitHub Issue Title with AzDO Work Item ID
        uses: actions/github-script@v6
        with:
          script: |
            const issueNumber = context.payload.issue.number;
            const workItemId = '${{ steps.create-work-item.outputs.work_item_id }}';
            const currentTitle = context.payload.issue.title;
            if (!currentTitle.includes('[AzDO Work Item:')) {
              const newTitle = `[AzDO Work Item: ${workItemId}] ${currentTitle}`;
              console.log(`Updating issue title to: ${newTitle}`);
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                title: newTitle
              });
            }

      - name: Add comment on GitHub Issue
        uses: actions/github-script@v6
        env:
          WORK_ITEM_ID: ${{ steps.create-work-item.outputs.work_item_id }}
          WORK_ITEM_URL: ${{ steps.create-work-item.outputs.work_item_url }}
        with:
          script: |
            const issueNumber = context.payload.issue.number;
            const issueCreator = context.payload.issue.user.login;
            const workItemId = process.env.WORK_ITEM_ID;
            const workItemUrl = process.env.WORK_ITEM_URL;
            const commentBody = `Thank you for posting, @${issueCreator}! The NI development team has been notified and a corresponding work item has been created internally.\n\n**Azure DevOps Work Item:** [#${workItemId}](${workItemUrl})\n\nThe team will review and reply back if any further clarification is required.`;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: commentBody,
            });

      - name: Send Teams Notification
        env:
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_URL: ${{ github.event.issue.html_url }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          WORK_ITEM_ID: ${{ steps.create-work-item.outputs.work_item_id }}
          WORK_ITEM_URL: ${{ steps.create-work-item.outputs.work_item_url }}
          WORK_ITEM_TYPE: ${{ steps.determine-work-item-type.outputs.display_type }}
        run: |
          PAYLOAD=$(jq -n \
            --arg issueTitle "$ISSUE_TITLE" \
            --arg issueUrl "$ISSUE_URL" \
            --arg issueNumber "$ISSUE_NUMBER" \
            --arg workItemId "$WORK_ITEM_ID" \
            --arg workItemUrl "$WORK_ITEM_URL" \
            --arg workItemType "$WORK_ITEM_TYPE" \
            '{
              "@type": "MessageCard",
              "@context": "http://schema.org/extensions",
              "summary": "New GitHub Issue",
              "themeColor": "0078D4",
              "title": "New GitHub Issue Received",
              "sections": [{
                "facts": [
                  {"name": "GitHub Issue:", "value": "[#\($issueNumber): \($issueTitle)](\($issueUrl))"},
                  {"name": "AzDO Work Item ID:", "value": "[\($workItemId)](\($workItemUrl))"},
                  {"name": "AzDO Work Item Type:", "value": $workItemType}
                ]
              }]
            }')

          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" \
            -X POST \
            -H "Content-Type: application/json" \
            --data "$PAYLOAD" \
            "$TEAMS_WEBHOOK_URL")

          if [ "$RESPONSE" -ne 200 ] && [ "$RESPONSE" -ne 202 ]; then
            echo "Failed to send Teams notification. HTTP status: $RESPONSE"
            exit 1
          fi

          echo "Teams notification sent successfully."

  sync-comment-to-azdo:
    if: >
      github.event_name == 'issue_comment' &&
      github.event.action == 'created' &&
      github.event.comment.user.login != 'github-actions[bot]' &&
      github.event.comment.user.type != 'Bot'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Extract Work Item ID from Issue Title
        id: extract-work-item
        uses: actions/github-script@v6
        with:
          script: |
            const issue = await github.rest.issues.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number
            });
            const match = issue.data.title.match(/\[AzDO Work Item:\s*(\d+)\]/);
            if (match) {
              console.log(`Found Work Item ID: ${match[1]}`);
              return match[1];
            }
            console.log('No Work Item ID found in issue title');
            return null;
          result-encoding: string

      - name: Sync comment to Azure DevOps
        if: steps.extract-work-item.outputs.result != 'null'
        env:
          AZDO_ORG: ${{ secrets.AZDO_ORG }}
          AZDO_PROJECT: ${{ secrets.AZDO_PROJECT }}
          AZURE_DEVOPS_TOKEN: ${{ secrets.AZURE_DEVOPS_TOKEN }}
          WORK_ITEM_ID: ${{ steps.extract-work-item.outputs.result }}
          COMMENT_USER: ${{ github.event.comment.user.login }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          COMMENT_CREATED_AT: ${{ github.event.comment.created_at }}
          COMMENT_URL: ${{ github.event.comment.html_url }}
        run: |
          echo "Syncing comment to AzDO Work Item #$WORK_ITEM_ID"

          ESCAPED_COMMENT_BODY=$(echo "$COMMENT_BODY" | sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g; s/"/\&quot;/g' | sed ':a;N;$!ba;s/\n/<br\/>/g')

          FORMATTED_COMMENT="<div style=\"border-left: 3px solid #0078d4; padding: 10px; margin: 10px 0; background-color: #f3f4f6;\"><strong>GitHub Comment by @${COMMENT_USER}</strong><br/><em>${COMMENT_CREATED_AT}</em><br/><a href=\"${COMMENT_URL}\">View on GitHub</a><br/><br/>${ESCAPED_COMMENT_BODY}</div>"

          PATCH_DOC=$(jq -n \
            --arg comment "$FORMATTED_COMMENT" \
            '[{"op": "add", "path": "/fields/System.History", "value": $comment}]')

          echo "$PATCH_DOC" > patch-comment.json

          WORK_ITEM_API_URL="https://dev.azure.com/$AZDO_ORG/$AZDO_PROJECT/_apis/wit/workitems/$WORK_ITEM_ID?api-version=6.0"

          RESPONSE=$(curl -s -u :$AZURE_DEVOPS_TOKEN \
            -X PATCH \
            -H "Content-Type: application/json-patch+json" \
            --data @patch-comment.json \
            "$WORK_ITEM_API_URL")

          if echo "$RESPONSE" | jq -e '.id' > /dev/null; then
            echo "Comment synced to Work Item #$WORK_ITEM_ID"
          else
            echo "Failed to sync comment"
            echo "Response: $RESPONSE"
            exit 1
          fi